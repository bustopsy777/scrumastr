apiVersion: v1
kind: Service
metadata:
  name: cs
  labels:
    app: cs
spec:
  selector:
    app: cs
  type: NodePort
  ports:
    - name: cs-frontend
      port: 5100
      targetPort: 5100
      nodePort: 31510
    - name: cs-backend
      port: 5000
      targetPort: 5000
      nodePort: 31500
