apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chatscrum.fullname" . }}-test-connection"
  labels:
    {{- include "chatscrum.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chatscrum.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
